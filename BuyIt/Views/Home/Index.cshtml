


<script>
    function deleteCharacter(str, x) {
        var lastChar = str[str.length - 1];

        if (lastChar == x) {
            var newStr = str.substring(0, str.length - 1);
            console.log(newStr);
        }
    }

    deleteCharacter("deneme", e) 

</script>